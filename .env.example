# mdash — Docker Compose environment variables
# Copy this file to .env and adjust as needed.

# Port the server listens on (inside and outside the container)
PORT=3000

# Directory inside the container where documents are persisted
STORAGE_DIR=/data

# Public URL used for generating document links (e.g., in PUT /new responses)
BASE_URL=http://localhost:3000
